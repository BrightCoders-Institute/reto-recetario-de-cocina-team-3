import React from 'react';
import { View } from 'react-native';
import { Title } from '../components/Title';

export const Home = () => {
	return (
		<View>
			<Title title='Test' />
		</View>
	);
};
